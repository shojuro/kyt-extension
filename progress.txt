# KYT Validation Sprint - Progress Tracker
# Last Updated: 2026-02-17

## Current Branch: feature/history-import

## ✅ Entity Recency Resolution v2 — "Jerry Problem" Fix (2026-02-17)

### Problem: 3-Step Jerry Test Failed
User ran a cross-platform test: (1) Claude "jerry is now real" → retrieved old meta-conversation, (2) ChatGPT "Jerry is _____" → stale "not real" answer, (3) ChatGPT retry "best friend of 60+ years" → finally correct. The v1 recency resolution we shipped was live but failed the real-world test.

### Root Cause Analysis: 5 Compounding Failures
1. **Fatal candidate pool size** — `topK=3` sent to edge function, only 3 candidates ever reached client-side pipeline. Newest factual correction often not in pool.
2. **Semantic richness dominance** — 300-word meta-conversation about Jerry outscored 5-word "Jerry is now real" in embedding space. 15% recency multiplier couldn't close the gap.
3. **MMR entity dedup** — Kept only highest-scoring Jerry item, LLM never saw the contradiction.
4. **Negation regex structural failure** — Only triggered 1.5x boost for negations ("not real"). Affirmative corrections ("is now real") only got 1.15x. Broke on 3-step chains.
5. **Edge path timestamps were `undefined`** — `edge-search.js` mapped `item.timestamp` (doesn't exist on RPC results) instead of `item.created_at`. Recency resolution was a complete no-op on the edge path.

### Fix: 4-Part Structural Overhaul

**A1: Decouple candidate pool from injection cap** (client + server)
- New `candidatePoolSize: 15` in contextConfig (retrieval pool) vs `maxContextItems: 3` (injection cap)
- Server `rerankAndFilter()` uses `returnCount` param instead of hardcoded `slice(0, 5)`
- `vectorSearchCount = Math.max(topK, 20)` ensures reranker always gets 20+ candidates
- Both edge and legacy search paths updated

**A2: Pure timestamp ordering — drop negation heuristic** (client)
- `applyRecencyResolution()` rewritten: newest per entity gets **1.5x**, ALL older get **0.6x**
- No negation regex. Handles affirmation chains correctly: "is real" → "not real" → "now real"
- Prefers server-provided entity data (`item.entities[]`) over regex `extractEntities()`

**B1: Entity-timeline guarantee** (server — new RPC + pipeline step)
- New migration `20260217100000_entity_timeline_guarantee.sql`
- RPC `get_newest_turns_for_entities(p_entity_ids, p_user_id, p_exclude_turn_ids, p_max_per_entity)`
- After vector+RRF merge (Step 5b), queries `entity_mentions` for newest chat_turn per entity
- If newest mention was missed by semantic search, injects it into candidate pool before reranking
- **Deployed to Supabase and tested** — returns correct results, exclusion logic verified

**B2: Pipe server entity data to client** (server + client)
- New `enrichWithEntities()` in `get_relevant_memories.ts` — batch-queries `entity_mentions`, attaches `entities[]` array with canonical names to each result
- `edge-search.js` maps `item.entities` through to client
- `applyRecencyResolution` uses server entities (GPT-4o-mini extraction) instead of fragile regex

**Bonus: Edge path timestamp fix** (client)
- `edge-search.js`: `item.created_at || item.timestamp || item.start_timestamp` (was `item.timestamp || item.start_timestamp`)
- This alone would have fixed a major part of the Jerry problem — recency resolution needs working timestamps

### Files Modified
| File | Change | Risk |
|------|--------|------|
| `background.js` | `candidatePoolSize: 15`, pure timestamp recency resolution, use server entities | Medium |
| `src/edge-search.js` | Map `created_at` for timestamps, map `entities[]` | Low |
| `supabase/functions/_shared/get_relevant_memories.ts` | `vectorSearchCount`, `returnCount`, entity timeline guarantee (Step 5b), `enrichWithEntities()` (Step 7) | Medium |
| `supabase/functions/search_memories/index.ts` | Pipeline comment update only | None |
| `supabase/migrations/20260217100000_entity_timeline_guarantee.sql` | New RPC `get_newest_turns_for_entities` | Low |

### Server Pipeline (updated)
1. Embed → 2. Entity search + HyDE + concept detection (parallel) → 3. Graph walk → 4. HyDE short-circuit check → 5. Dual vector search → 5a. RRF merge → **5b. Entity timeline guarantee** → 6. Rerank + BM25 + confidence filter → **7. Entity enrichment**

### Client Pipeline (updated)
1. Recency multiplier (15%) → 2. Recursion guard → 3. Meta filter → 4. Deflection/echo penalty → 5. Dedup → **6. Entity-aware recency resolution (pure timestamp, server entities)** → 7. MMR(3) → 8. Keyword boost → 9. Confidence filter → 10. Injection (newest-first)

### Edge Function: search_memories v29 deployed

### Verification
1. Reload extension from chrome://extensions
2. Store conflicting facts: "X is A" then "X is B" then "X is A again"
3. Service worker console: `🕐 Recency boost: "X" — newest item boosted 1.5x`
4. Edge function logs: `Entity timeline: injected N newest mentions into candidate pool`
5. Result should always reflect the most recent statement, regardless of affirmation/negation

---

## Previous: Recency-Aware Contradiction Resolution v1 (2026-02-17)

### Layered Fix (3 files) — superseded by v2 above

**Layer 1: Mild recency multiplier on all search results** (`background.js`)
- Exponential decay: 15% weight, 30-day half-life — *still active in v2*

**Layer 2: Entity-aware contradiction resolution** (`background.js`)
- Negation-based heuristic — *replaced by pure timestamp ordering in v2*

**Layer 3: Recency directive + chronological sort in injection builder** (`kyt-memory-injection-builder.js`)
- Items sorted newest-first, LLM directives for recency — *still active in v2*

---

## ✅ Advanced GraphRAG Implementation (2026-02-14)

### Problem: Concept Labeling Gap
User remembers "the walking analogy" but stored text says "you don't tell a child who took 4 steps to scratch everything." Embedding models cannot bridge this gap — it's a concept labeling problem. GraphRAG is the solution: GPT-4o-mini labels concepts at ingest, graph walk retrieves by label at query time.

### Phase 0: Bug Fixes
- **0A: entities_extracted flag** — `save_chat_turn/index.ts` + `save_chat_turn_batch/index.ts`: After `saveEntitiesWithMentions()` succeeds, sets `chat_turns.entities_extracted = true`. Without this, backfill can't know which turns already had extraction.
- **0B: Vector dimension mismatch** — Migration `20260216000000`: `find_similar_entities` RPC changed from `VECTOR(1536)` to `VECTOR(4096)` (Qwen3-Embedding-8B). Entity resolution depends on this.

### Phase 1: Text-Based Entity Search Fallback
- Migration `20260216000001`: Enables `pg_trgm` extension, creates GIN trigram indexes on `entities.canonical_name/normalized_name/entity_text`, adds `search_entities_by_text()` RPC with 3 strategies (trigram similarity, ILIKE substring, keyword splitting).
- `get_relevant_memories.ts`: `searchEntities()` falls back to text search when embedding search returns 0.
- `browser-search.js`: `searchGraphWalk()` adds text entity search fallback client-side.

### Phase 2+3: Multi-Hop Graph Walk + Temporal Decay
- Migration `20260216000002`: `temporal_decay_factor()` helper (half-life 90 days). Recursive `graph_walk_from_entities()` with `p_max_depth` and `p_max_intermediate` params. Cumulative strength = product of edge strengths × temporal decay.
- Both server and client integration pass `p_max_depth: 2, p_max_intermediate: 20`.

### Phase 4: Relationship Classification
- Migration `20260216000003`: Adds `relationship_type` column to `entity_relationships` + index. Updates `upsert_entity_relationship` with 4th param; "keep more-specific type" conflict logic.
- `entity-extractor.ts`: New `classifyRelationshipType()` derives type from entity metadata (family_of, works_with, illustrates, discussed_together, co_occurrence).

### Phase 5: Entity Resolution (Fuzzy Dedup)
- `entity-extractor.ts`: After exact canonical_name match fails, calls `find_similar_entities()` (4096-dim). Merges only if same `entity_type` + same relationship suffix + similarity >= 0.90. Prevents "Jenn"/"Jennifer_trainer" duplication while keeping "Jennifer_trainer"/"Jennifer_sister" distinct.

### Phase 6: Query-Time Concept Detection
- `get_relevant_memories.ts`: New `detectConceptEntities()` uses text search to find CONCEPT/ANALOGY/THEME entities. Runs in parallel with entity search + HyDE. Concept entity IDs merged into `boostEntityIds` for graph walk.

### Phase 7: Entity Re-Extraction Backfill
- New edge function `backfill_entities/index.ts`: Queries `entities_extracted = false`, runs `extractEntities()` + `saveEntitiesWithMentions()` per row. Batch size 5, max 100/invocation, 2s delay between batches.
- `background.js`: Added `KYT_DEBUG.backfillEntities()` debug command.

### New Files
| File | Purpose |
|------|---------|
| `supabase/migrations/20260216000000_fix_find_similar_entities_dims.sql` | Fix VECTOR(1536)→4096 |
| `supabase/migrations/20260216000001_text_entity_search.sql` | pg_trgm + search_entities_by_text RPC |
| `supabase/migrations/20260216000002_multi_hop_graph_walk.sql` | Recursive graph walk + temporal decay |
| `supabase/migrations/20260216000003_relationship_classification.sql` | relationship_type column + updated upsert |
| `supabase/functions/backfill_entities/index.ts` | Entity re-extraction backfill |

### Verification Plan
1. Apply migrations sequentially via Supabase
2. `SELECT * FROM search_entities_by_text('walking analogy', '<uid>', 5)` — should match ANALOGY entities
3. `SELECT temporal_decay_factor(NOW() - INTERVAL '90 days')` → ~0.50
4. `SELECT * FROM graph_walk_from_entities(ARRAY['<id>']::UUID[], '<uid>', 10, 2, 20)` — depth-2 results
5. Call `backfill_entities` edge function → verify entities_extracted flipped
6. Search "walking analogy" → concept detection finds ANALOGY entity → graph walk retrieves original chat_turn

---

## ✅ SUCCESSFUL CHECKPOINT: Cross-Platform Retrieval Verified (2026-02-12)

### Bidirectional RAG Injection — CONFIRMED WORKING
- **Claude → ChatGPT**: User tested query on ChatGPT, retrieved memories from Claude sessions. Correct, specific information returned.
- **ChatGPT → Claude**: User tested query on Claude, retrieved memories from ChatGPT sessions. Correct, specific information returned.
- **Different topics, no overlap** — retrieval precision confirmed across platforms.

### Fixes Applied This Session (6 changes across 7 files):

1. **Queue Manager load fix (CRITICAL)** — `manifest.json`: Added `src/utils/normalize-platform.js` to `web_accessible_resources`. Queue-manager.js imports this module; without it, Chrome blocked the import and the entire ChatGPT queue manager crashed silently. Messages from ChatGPT were not syncing to Supabase at all.

2. **Sync-before-search fire-and-forget** — `background.js:1059-1069`: Changed `await withTimeout(executeDebouncedSync(), 3000)` to fire-and-forget. Saves 0-3s from the critical search path. Messages still sync via alarm; this just stops the sync from blocking context retrieval.

3. **Background timeout 8s → 12s** — `background.js:1568-1574`: GET_CONTEXT `Promise.race` timeout increased from 8000ms to 12000ms. With parallelized search (~4-7s) + fire-and-forget sync, 12s provides comfortable headroom.

4. **MAIN world timeout 10s → 15s** — `platforms/chatgpt/inject.js:336-345` and `platforms/claude/content_test.js:350-360`: Both platform inject scripts increased from 10s to 15s, maintaining 3s margin after the 12s background timeout.

5. **Query expansion NaN guard** — `src/query-expansion.js:130`: Added `if (isNaN(numericValue)) continue;` to prevent `$NaN` garbage variants when currency regex matches commas in natural text (e.g., "blonde woman, white bra").

6. **Search parallelization** — `src/browser-search.js`: Supabase text + semantic search already parallelized via `Promise.all` (confirmed in place, no change needed).

### Test Results
- 215 passed, 5 skipped, 3 failed (pre-existing: history-import.spec.js embedding dimensions + timing)
- No regressions from this session's changes.

### Next: Extended Validation (20 queries + edge cases)
- Short throwaway comments vs. deep multi-turn discussions
- Topic switches mid-conversation
- Ambiguous queries where confidence should be low

---

## Previous Work: Claude Bridge Queue-Manager Resilience (2026-02-11)

### Claude Bridge: Queue-Manager Integration - COMPLETE
- **Problem**: After extension reload, `content_bridge.js` drops captured messages silently
  when `chrome.runtime.id` becomes undefined (context invalidation). ChatGPT platform
  already handled this via `queue-manager.js`; Claude bridge had no fallback.
- **Fix**: Static import of queue-manager into `platforms/claude/content_bridge.js`
  with module content script (`"type": "module"` in manifest).
- **CSP fix**: Initial dynamic `import()` failed on claude.ai because classic content
  scripts route module fetches through the host page's CSP, which blocks
  `chrome-extension://` origins. Switching to `"type": "module"` makes Chrome resolve
  imports at injection time via the extension's own loader, bypassing page CSP.
- **Simplification**: Since `queueManager` is always available via static import,
  the handler is now a single `queueManager.capture(messageData)` call. The queue
  manager handles the full fallback chain internally:
  1. `chrome.runtime.sendMessage` to background (context valid)
  2. Encrypted `chrome.storage.local` (context invalid, crypto key available)
  3. Unencrypted `chrome.storage.local` (no crypto key)
  4. `window.localStorage` emergency fallback (chrome.storage unavailable)
- **Recovery**: `background.js:processPendingLocalQueues()` already processes pending
  queues on startup, install, and alarm — no background changes needed.
- Files: `platforms/claude/content_bridge.js`, `manifest.json`
- Tests: 215 passed, 5 skipped, 3 pre-existing failures (embedding dimension + dedup flake in history-import.spec.js)

---

## Previous Work: Memory Retrieval Pipeline Fix (2026-02-06)

### Embedding Provider Migration: FIXED
- **Root cause**: HuggingFace migrated Qwen3-Embedding-8B from Nebius to Scaleway
- **URL fix**: `router.huggingface.co/nebius/` -> `router.huggingface.co/scaleway/`
- **Model ID fix**: Scaleway expects `qwen3-embedding-8b` (lowercase, no org prefix), not `Qwen/Qwen3-Embedding-8B`
- Files: `src/browser-sync.js`, `src/browser-search.js`

### Pipeline Resilience: IMPLEMENTED
1. **Shared circuit breaker** - Extracted to `src/embedding-circuit-breaker.js` (ES6 exports)
   - Both sync and search import shared module
   - Search skips embedding API instantly when circuit open (no 24s timeout waste)
2. **Semantic threshold** - `background.js` searchHybrid: 0.65 -> 0.50
   - Supabase RPC already applies `match_threshold`; client-side 0.65 was redundant double-filter
3. **Adaptive confidence** - BM25-only mode uses threshold 0.25 (vs 0.40 with semantic)
   - `searchHybrid()` returns `.metadata.semanticAvailable`
   - `getContextForInjection()` adapts threshold when semantic search unavailable
4. **MMR lambda** - 0.3 -> 0.5 (balanced relevance/diversity for entity-specific queries)

### DOM Role Mislabeling: FIXED
- **Root cause**: `inject.js:inferMessageRole()` checked `data-author` but ChatGPT uses `data-message-author-role`
- All fallback paths defaulted to `'user'`, so assistant messages stored as user
- **Fix**: Added `data-message-author-role` check (highest priority), changed default to `'assistant'`
- File: `platforms/chatgpt/inject.js`

### Verification Status
- [ ] Reload extension, trigger sync -> no 403/422 errors
- [ ] Search query -> semantic results returned
- [ ] DOM-captured assistant messages have role='assistant' in database
- [ ] BM25-only fallback works when embedding endpoint is down

---

## Previous Work: Fix Mobile Message Capture (ChatGPT + Claude)

### ChatGPT Status: VERIFIED ✅ (commit 06203f1)
### Claude Status: VERIFIED ✅ (commit 904cc4a)
### NOTE: Previous fix (2df2dd6) was applied to wrong file (inject.js)
### Fix (9bc546d) applied to correct file (content_test.js)
### Content array fix (141723b) - extracts text from content blocks array
### Role diagnostic (904cc4a) - confirmed user + assistant roles syncing correctly

### Problem Summary
ChatGPT Mobile Voice and Manual input are not being captured.
Extension initializes correctly but message capture mechanisms are failing.

### Root Causes Identified
1. Fetch Capture - Only metadata endpoints intercepted, not actual messages
2. **FIXED: ChatGPT Mobile Sync** - GET /conversation/{id} requests skipped (no body)
3. **FIXED: Claude Mobile Sync** - GET /chat_conversations/{id} requests skipped (no body)
4. WebSocket Voice - Only control messages captured, no transcripts
5. DOM Observer - Selectors may be outdated after UI changes

### Implementation Steps
[x] Phase 1: Investigation - Analyze console logs, read inject.js/dom-observer.js
[x] Phase 2: Plan - Design diagnostic-first fix approach
[x] Step 1: Add diagnostics to understand current ChatGPT API structure
[x] Step 2: Test extension - gather diagnostic data from user
[x] Step 3: Analyze findings - identified GET requests not processed
[x] Step 4: FIXED - Add GET /conversation handler (commit 06203f1)
    - Detect GET /backend-api/conversation/{id} without body
    - Process JSON response through processConversationTree()
    - Add 'fetch_tree' to deduplication confidence map (95%)
[x] Step 5: User verification - PASSED
    - Mobile-synced message: "Testing typing in mobile 1242am" ✅
    - Voice message: captured and synced ✅
    - Typed message: captured and synced ✅
    - HyDE questions generated ✅
    - Embeddings generated via HuggingFace ✅
[ ] Step 6: Remove diagnostic code (optional - can keep for future debugging)

### Claude Mobile Investigation (In Progress)
[x] Step 1: Applied GET handler to correct file (content_test.js, not inject.js)
[x] Step 2: User tested - GET handler fires but no messages captured
[x] Step 3: Added enhanced diagnostics (commit 850e27e)
    - Field name detection: text, content, message, body
    - Per-message structure logging
    - Processing summary with capture/skip counts
[x] Step 4: Analyzed diagnostic output - identified root cause
    - `contentType: 'object'` - msg.content is array of content blocks
    - Format: [{ type: 'text', text: 'actual message' }]
    - All 8 messages skipped because .trim() fails on object
[x] Step 5: Applied fix (commit 141723b)
    - Parse content array to extract text from content blocks
    - Handle both string and array-of-blocks format
[x] Step 6: User verification - PASSED (commit 904cc4a)
    - Added role distribution logging to sync
    - Supabase confirmed both role=user and role=assistant inserted
    - Mobile text input: captured and synced ✅
    - Mobile dictation: captured and synced ✅
    - Mobile voice: captured and synced ✅

### Files Modified
- platforms/chatgpt/inject.js (Fetch/WebSocket capture) - FIXED
- platforms/chatgpt/dom-observer.js (DOM fallback capture)
- platforms/claude/content_test.js (Mobile sync GET handler) - FIXED (9bc546d)

### Plan File
/home/penguinzyue/.claude/plans/eventual-nibbling-sprout.md

---

## Recent Completed Work

### Memory Retrieval Pipeline Fix (2026-02-06) - IN VERIFICATION
- Embedding provider: Nebius -> Scaleway (model ID: qwen3-embedding-8b)
- Shared circuit breaker: src/embedding-circuit-breaker.js
- Adaptive confidence: 0.40 (semantic) / 0.25 (BM25-only)
- Semantic threshold: 0.65 -> 0.50
- MMR lambda: 0.3 -> 0.5
- DOM role fix: data-message-author-role check added to inject.js

### RAG System Fix (2025-12-28) - COMPLETE
- Embedding endpoint: api.studio.nebius.ai -> router.huggingface.co/nebius
- Reranking: BGE reranker -> Jina Reranker API (free tier)
- Confidence filter: 0.70 -> 0.40 threshold
- Setup UI: Added Jina API key field
- Commits: 905b306, 27ffebe, 64b9bf8

### History Import Feature (2025-12-08) - VALIDATED
- Database: 3,397 chat_turns, 1,378 messages
- Edge function: save_chat_turn_batch working
- Popup UI: Import modal fully wired
- Test status: All 218 tests pass

---

## Test Results Summary
- Total test files: 14 passed, 1 failed (pre-existing embedding dimension issue)
- Total tests: 215 passed, 5 skipped, 3 failed (pre-existing: embedding dims + timing)
- Last run: 2026-02-12
